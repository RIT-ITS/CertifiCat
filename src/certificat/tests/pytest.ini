[pytest]
addopts = --reuse-db -m 'not slow' --cov-config=.coveragerc
markers =
    slow: marks tests as slow (deselect with '-m \"not slow\"')
python_files = tests.py test_*.py *_tests.py
pythonpath = .
filterwarnings =
    error        
    ignore::DeprecationWarning
